
import NavigationBar from "@/components/NavigationBar";
import RecipeRevamp from "@/components/RecipeRevamp";

const RecipeRevampPage = () => {
  return (
    <div className="min-h-screen bg-surface">
      <NavigationBar />
      <RecipeRevamp />
    </div>
  );
};

export default RecipeRevampPage;
